function Pk = known_dynamics(dynamics_x,dynamics_f)
% Compute the known dynamics transition matrix

Pk = zeros(length(bufferStates),length(channelStates),length(cardStates),...
            length(BEPActions),length(cardActions),length(throughputActions),...
            length(bufferStates),length(channelStates),length(cardStates)


end

